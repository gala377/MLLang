
extend seq {
  find: do s pred:
    let res = filter s pred
    if eq? 0 (seq.len res):
      none
    else:
      fst res,

  empty?: do s -> eq? (seq.len s) 0,
  notempty?: do s -> not (seq.empty? s),
}